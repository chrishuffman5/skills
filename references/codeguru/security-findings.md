# Security Findings

> **Note:** CodeGuru Security reached end of support in November 2025. These commands use `aws codeguru-security`.

### 5.1 `get-findings`

Returns a list of all findings generated by a specific scan. Uses `aws codeguru-security`. **Paginated operation.**

**Synopsis:**
```bash
aws codeguru-security get-findings \
    --scan-name <value> \
    [--status <value>] \
    [--starting-token <value>] \
    [--page-size <value>] \
    [--max-items <value>] \
    [--cli-input-json | --cli-input-yaml] \
    [--generate-cli-skeleton <value>]
```

**Parameters:**

| Parameter | Required | Type | Default | Description |
|-----------|----------|------|---------|-------------|
| `--scan-name` | **Yes** | string | -- | Name of the scan to retrieve findings for |
| `--status` | No | string | None | Filter by finding status: `Open`, `Closed`, `All` |
| `--starting-token` | No | string | None | Pagination token from previous response |
| `--page-size` | No | integer | None | Items per API call |
| `--max-items` | No | integer | None | Total items to return |

**Output Schema:**
```json
{
    "findings": [
        {
            "createdAt": "timestamp",
            "description": "string",
            "detectorId": "string",
            "detectorName": "string",
            "detectorTags": ["string"],
            "generatorId": "string",
            "id": "string",
            "remediation": {
                "recommendation": {
                    "text": "string",
                    "url": "string"
                },
                "suggestedFixes": [
                    {
                        "code": "string",
                        "description": "string"
                    }
                ]
            },
            "resource": {
                "id": "string",
                "subResourceId": "string"
            },
            "ruleId": "string",
            "severity": "Critical|High|Medium|Low|Info",
            "status": "Open|Closed|All",
            "title": "string",
            "type": "string",
            "updatedAt": "timestamp",
            "vulnerability": {
                "filePath": {
                    "codeSnippet": [
                        {
                            "content": "string",
                            "number": "integer"
                        }
                    ],
                    "endLine": "integer",
                    "name": "string",
                    "path": "string",
                    "startLine": "integer"
                },
                "id": "string",
                "itemCount": "integer",
                "referenceUrls": ["string"],
                "relatedVulnerabilities": ["string"]
            }
        }
    ],
    "nextToken": "string"
}
```

---

### 5.2 `batch-get-findings`

Returns a list of requested findings from one or more scans. Uses `aws codeguru-security`.

**Synopsis:**
```bash
aws codeguru-security batch-get-findings \
    --finding-identifiers <value> \
    [--cli-input-json | --cli-input-yaml] \
    [--generate-cli-skeleton <value>]
```

**Parameters:**

| Parameter | Required | Type | Default | Description |
|-----------|----------|------|---------|-------------|
| `--finding-identifiers` | **Yes** | list | -- | List of finding identifiers. Shorthand: `scanName=string,findingId=string ...` |

**Output Schema:**
```json
{
    "findings": [
        {
            "createdAt": "timestamp",
            "description": "string",
            "detectorId": "string",
            "detectorName": "string",
            "detectorTags": ["string"],
            "generatorId": "string",
            "id": "string",
            "remediation": {
                "recommendation": {
                    "text": "string",
                    "url": "string"
                },
                "suggestedFixes": [
                    {
                        "code": "string",
                        "description": "string"
                    }
                ]
            },
            "resource": {
                "id": "string",
                "subResourceId": "string"
            },
            "ruleId": "string",
            "severity": "Critical|High|Medium|Low|Info",
            "status": "Open|Closed|All",
            "title": "string",
            "type": "string",
            "updatedAt": "timestamp",
            "vulnerability": {
                "filePath": {
                    "codeSnippet": [],
                    "endLine": "integer",
                    "name": "string",
                    "path": "string",
                    "startLine": "integer"
                },
                "id": "string",
                "itemCount": "integer",
                "referenceUrls": ["string"],
                "relatedVulnerabilities": ["string"]
            }
        }
    ],
    "failedFindings": [
        {
            "scanName": "string",
            "findingId": "string",
            "errorCode": "DUPLICATE_IDENTIFIER|ITEM_DOES_NOT_EXIST|INTERNAL_ERROR|INVALID_FINDING_ID|INVALID_SCAN_NAME"
        }
    ]
}
```

---

### 5.3 `list-findings-metrics`

Returns metrics about findings over time. Uses `aws codeguru-security`. **Paginated operation.**

**Synopsis:**
```bash
aws codeguru-security list-findings-metrics \
    --start-date <value> \
    --end-date <value> \
    [--starting-token <value>] \
    [--page-size <value>] \
    [--max-items <value>] \
    [--cli-input-json | --cli-input-yaml] \
    [--generate-cli-skeleton <value>]
```

**Parameters:**

| Parameter | Required | Type | Default | Description |
|-----------|----------|------|---------|-------------|
| `--start-date` | **Yes** | timestamp | -- | Start date for metrics retrieval (ISO 8601 format) |
| `--end-date` | **Yes** | timestamp | -- | End date for metrics retrieval (ISO 8601 format) |
| `--starting-token` | No | string | None | Pagination token from previous response |
| `--page-size` | No | integer | None | Items per API call |
| `--max-items` | No | integer | None | Total items to return |

**Output Schema:**
```json
{
    "findingsMetrics": [
        {
            "date": "timestamp",
            "closedFindings": {
                "critical": "double",
                "high": "double",
                "medium": "double",
                "low": "double",
                "info": "double"
            },
            "newFindings": {
                "critical": "double",
                "high": "double",
                "medium": "double",
                "low": "double",
                "info": "double"
            },
            "openFindings": {
                "critical": "double",
                "high": "double",
                "medium": "double",
                "low": "double",
                "info": "double"
            },
            "meanTimeToClose": {
                "critical": "double",
                "high": "double",
                "medium": "double",
                "low": "double",
                "info": "double"
            }
        }
    ],
    "nextToken": "string"
}
```

---

### 5.4 `get-metrics-summary`

Returns a summary of metrics for an account. Uses `aws codeguru-security`.

**Synopsis:**
```bash
aws codeguru-security get-metrics-summary \
    --date <value> \
    [--cli-input-json | --cli-input-yaml] \
    [--generate-cli-skeleton <value>]
```

**Parameters:**

| Parameter | Required | Type | Default | Description |
|-----------|----------|------|---------|-------------|
| `--date` | **Yes** | timestamp | -- | Date for the metrics summary (ISO 8601 format) |

**Output Schema:**
```json
{
    "metricsSummary": {
        "date": "timestamp",
        "categoriesWithMostFindings": [
            {
                "categoryName": "string",
                "findingNumber": "integer"
            }
        ],
        "openFindings": {
            "critical": "double",
            "high": "double",
            "medium": "double",
            "low": "double",
            "info": "double"
        },
        "scansWithMostOpenCriticalFindings": [
            {
                "scanName": "string",
                "findingNumber": "integer"
            }
        ],
        "scansWithMostOpenFindings": [
            {
                "scanName": "string",
                "findingNumber": "integer"
            }
        ]
    }
}
```
